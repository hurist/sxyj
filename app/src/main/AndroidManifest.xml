<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ffcc66.sxyj">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/my_luncher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/my_luncher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="adjustPan|stateHidden" />
        <activity android:name=".login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.RegisterActivity" />
        <activity android:name=".bookstore.BookDetailActivity" />
        <activity android:name=".bookstore.bookcategary.BookCategaryActivity" />
        <activity android:name=".bookstore.BookRankingActivity" />
        <activity android:name=".bookstore.booklist.BookListActivity" />
        <activity android:name=".bookstore.bookcategary.BookCategaryListActivity"></activity>
    </application>

</manifest>